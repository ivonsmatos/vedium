def get_context(context):
    context.title = "Comunidade por idioma e nível — Vedium"
    context.description = "Entre na lista de interesse da comunidade Vedium por idioma e nível."
    context.no_cache = 1
    return context
