def get_context(context):
    context.title = "Referral Program — Vedium"
    context.description = (
        "Vedium's referral program is managed inside the Frappe platform, "
        "with logged-in access and team validation."
    )
    context.no_cache = 1
    context.lang = "en"
    context.canonical_url = "https://vediums.com/en/programa-de-indicacao"
    context.alt_lang_url = "https://vediums.com/programa-de-indicacao"
    return context
