def get_context(context):
    context.title = "Plans — Vedium"
    context.description = (
        "Compare Vedium's live class plans by frequency, goal and study pace "
        "before you move on to enrollment."
    )
    context.no_cache = 1
    context.lang = "en"
    context.canonical_url = "https://vediums.com/en/planos"
    context.alt_lang_url = "https://vediums.com/planos"
    return context
