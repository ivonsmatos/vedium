def get_context(context):
    context.title = "Diagnostic Class — Vedium"
    context.description = (
        "Book a diagnostic class to confirm your level, goal and best plan "
        "before enrolling at Vedium."
    )
    context.no_cache = 1
    context.lang = "en"
    context.canonical_url = "https://vediums.com/en/aula-diagnostica"
    context.alt_lang_url = "https://vediums.com/aula-diagnostica"
    return context
