def get_context(context):
    context.title = "Enrollment — Vedium"
    context.description = (
        "Choose your language, level and plan before moving on to the Vedium "
        "platform, where login and checkout are completed securely."
    )
    context.no_cache = 1
    context.lang = "en"
    context.canonical_url = "https://vediums.com/en/matricula"
    context.alt_lang_url = "https://vediums.com/matricula"
    return context
