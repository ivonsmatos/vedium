def get_context(context):
    context.title = "Brazilian Portuguese Placement Test — Vedium"
    context.description = (
        "Free Brazilian Portuguese placement test: grammar, reading, listening, "
        "writing and speaking to estimate your CEFR level from A1 to C1."
    )
