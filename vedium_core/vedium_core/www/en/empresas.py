def get_context(context):
    context.title = "Vedium for companies — B2B"
    context.description = "Language training for teams, with diagnostics, tracks and follow-up."
    context.no_cache = 1
    context.lang = "en"
    context.canonical_url = "https://vediums.com/en/empresas"
    context.alt_lang_url = "https://vediums.com/empresas"
    return context
