def get_context(context):
    context.title = "Verificar certificado — Vedium"
    context.description = "Verifique a autenticidade de certificados Vedium por código."
    context.no_cache = 1
    return context
