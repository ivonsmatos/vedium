# 11 — Matriz de migração de componentes (legado → V2)

> **Fase B (2026-08-24).** Nenhuma migração foi executada -- este documento é o PLANO. Nenhuma página listada abaixo foi alterada nesta fase. "Fase futura" usa nomenclatura aberta (C, D, E...) porque a sequência exata de aprovação ainda não foi definida; a ordem aqui reflete risco/dependência, não um cronograma comprometido.

| Componente atual | → Componente v2 | Rota(s) onde aparece | Dependências | Risco | Fase futura |
|---|---|---|---|---|---|
| `templates/includes/site_navbar.html` | Header v2 (`templates/includes/v2/header.html`) | Todas as ~336 URLs do sitemap | Login/registro (`app.vediums.com`), CTA de teste de nível dinâmico (`vd_level_test_url_override`/`_contact_override`), seletor de idioma (`data-vd-nav-urls`/`vedium-language.js`), WhatsApp, GTM | **MUITO ALTO** -- template mais compartilhado do site; troca errada quebra navegação em todas as páginas de uma vez | C (piloto em 1 página isolada antes de qualquer rollout amplo) |
| `templates/includes/site_footer.html` | Footer v2 (`templates/includes/v2/footer.html`) | Todas | Mesmas do Header + snippet GTM (`GTM-P6Q2FXLK`) + legal PT-only hardcoded | **MUITO ALTO** -- GTM vive só no footer hoje; perder o snippet para todo o tracking do site | C (junto com Header, mesmo piloto) |
| CTA de teste de nível (lógica de override) | Contrato preservado no Header/Footer v2 (não é um componente visual isolado) | Toda página de curso (`curso.html`) + header/footer | `get_course_level_destination()` (`course_urls.py`) | **ALTO** -- se a variável de override parar de ser propagada, o botão volta ao default errado silenciosamente (Iorubá/Espanhol/Hebraico ofereceriam teste que não existe) | C |
| Hero da Home (`page-header`/`page-header__title` em `www/index.html` + 5 traduções) | HeroFullBleed ou HeroSplit | `/`, `/en`, `/es`, `/fr`, `/de`, `/ru` | Grid de cursos ao vivo (LMS), CTAs de WhatsApp/teste, GTM `public_cta_click` (4 no PT) | ALTO -- página mais visível; achado da Fase A: `/en`,`/es`,`/fr`,`/de`,`/ru` hoje **não têm `<h1>`** -- migrar é oportunidade de corrigir, não só trocar visual | C |
| Bullets "Aulas 100% ao vivo" / "Professores nativos..." (lista fixa em `curso.html`/`index.html`) | FeatureCard | `curso.html`, `www/index.html` | Nenhuma (conteúdo estático) | BAIXO | C |
| Card de curso (`courses-one__single`) em `catalogo.html`, `www/index.html`, `marketing_landing.html` | CourseCard | `/cursos-de-idiomas-online`, home, ~60 landings (grid quando `LANDING_COURSE_FILTERS` aplica) | `get_published_courses()` (LMS Course ao vivo), evento `seo_landing_course_grid_click` | MÉDIO -- dado sempre ao vivo do LMS, risco concentrado em preservar regra "preço só com periodicidade" | C |
| `course-details__trail` (trilha Inglês A1-C1 / PLE Básico-Avançado) | LevelTimeline + LevelCard | `curso.html` (só cursos com trilha aprovada) | `get_course_navigation()` (`course_urls.py`) -- mesmo mecanismo corrigido/estendido na missão PLE desta sessão | MÉDIO -- lógica já é language-aware e testada (`test_pure_course_seo_navigation.py`); risco é só de apresentação | D |
| `course-details__instructor` (card de professor na ficha) | TeacherCard | `curso.html` (quando `course.instructors_list` não vazio) | `get_public_instructors()` (`vedium_core/courses.py`, filtra contas placeholder -- criado na missão PLE desta sessão) | MÉDIO -- lógica de dado já pronta e sem placeholder vazando; falta só foto/dado real pra maioria dos professores | D |
| Hero de landing (`vd-landing-hero` em `marketing_landing.html`) | HeroSplit ou HeroCentered | ~60 landings (pilares + objetivos, todos os idiomas) | `landing.alternates` (hreflang), Schema Course+FAQPage, eventos `seo_landing_*` | ALTO -- template único compartilhado por Inglês/Espanhol/Hebraico/Iorubá/PLE em todos os idiomas (mesmo padrão de risco de `curso.html`) | D |
| FAQ da landing (`vd-landing-section` com perguntas) | FAQAccordion | ~60 landings | JSON-LD `FAQPage` -- precisa continuar batendo com o texto visível (regra do 04) | MÉDIO | D |
| Seção de preço da landing (`vd-price-val`/`price_display`) | PricingCard (quando há planos) ou texto simples (quando é só referência) | ~60 landings | `landing.price_display`/`price_note` (estático, por slug) | BAIXO-MÉDIO | D |
| Card de post (`blog-card`) | BlogCard | `/blog`, `/blog/<categoria>`, `/en/blog`, `/es/blog` | Fonte dupla de dado: `Vedium Blog Post` (DocType) + `blog_content.BLOG_POSTS` (dict) -- achado da Fase A, não resolvido por este componente | MÉDIO -- risco é de dado (fonte dupla), não do componente em si | D |
| Formulário de intenção (Comunidade/B2B, `public_intent_page*.html`) | FormField + Input + Select + Alert | `/comunidade`, `/empresas` (+6 idiomas cada) | `public_funnel.submit_public_intent`, evento `public_intent_submit` → CRM Lead → Brevo | **ALTO** -- é geração de lead (P0/P1 em `conversion-contracts.md`); qualquer erro de `name`/endpoint quebra o funil silenciosamente | D |
| Botões de WhatsApp espalhados (dezenas de páginas) | Button (`variant="primary"`, `icon="whatsapp"`) | ~15+ páginas institucionais + header/footer + landings | Evento `public_cta_click` -- **achado de duplicação já documentado** (listener global + handler local disparam 2x por clique) | MÉDIO -- boa oportunidade de resolver a duplicação durante a própria migração, não só trocar o visual | D |
| Breadcrumb (`page-header__menu-list`) | Breadcrumb | `curso.html`, landings | Schema `BreadcrumbList` -- estrutura visual e JSON-LD precisam continuar derivando da mesma hierarquia | BAIXO | D |
| Passos "Como funciona" (`como-funciona.html`) | ProcessSteps | `/como-funciona` (+6 idiomas) | Nenhuma dinâmica | BAIXO | D |
| Formulário de teste de nível (5 páginas: `teste-de-nivel*.html`, `en/es/fr/de` placement test) | FormField/Input/Select (perguntas) + Alert (resultado) | `/teste-de-nivel`, `/teste-de-nivel-ingles`, `/en/portuguese-placement-test`, `/es/prueba-de-nivel-de-portugues`, `/fr/test-de-niveau-de-portugais`, `/de/portugiesisch-einstufungstest` | Cálculo de nível client-side (lógica própria, não portada por este design system), `public_funnel.save_placement_result`/`get_available_diagnostic_slots`, eventos `level_test_*` | **MUITO ALTO** -- funil de diagnóstico P0; migrar exige re-testar a lógica de cálculo em cada idioma, não só o visual | E |
| "Aula ao vivo" / demonstração de produto | VideoSection | Home, `/como-funciona` | **Sem mídia real hoje** (R01/R02 do shot list ainda não captados) | BAIXO agora (não há nada real pra quebrar) | E (aguarda captação real -- ver `09-real-vedium-shoot-list.md`, P0-LAUNCH) |
| Depoimentos | TestimonialCard | Nenhuma rota real hoje | **Sem depoimento aprovado publicável** (decisão pendente nº 4 de `04-design-system-plan.md`) | BAIXO agora | E (aguarda aprovação de depoimentos reais) |
| `/professores` (hoje só um 301 pra `/sobre`) | TeacherCard (grid) + TeacherProfileSummary | `/professores`, `/professores/<slug>` (páginas que ainda não existem) | Fotos/bios/consentimento de professores (decisão pendente nº 5-6 de `04-design-system-plan.md`) | N/A -- é criação nova, não migração | E (depende de aprovação de dados de professores) |
| Seletor de idioma (modal + `vedium-language.js`) | **Implementado na Fase B.6E** (Parte D da missão) -- LocaleMenu v2 (`templates/includes/v2/header.html` + `initLocaleMenu` em `design-system-v2.js`), ver `25-locale-switcher-contract.md`. Disclosure ancorado (não modal), só os 6 locales com conteúdo real publicado (pt-br/en/es/fr/de/ru -- não os 6 "irmãos regionais" do modal de produção). `vedium-language.min.js` REAL é reutilizado (não reimplementado) para a resolução de URL (`data-vd-nav-urls`/`data-vd-nav-current` no `<header>`, mesmo contrato). | Header v2 (`/design_system_v2`, `/design_system_v2_b2b`) | `vedium-language.min.js` (reutilizado via `<script>`, não portado/reescrito) | Risco original permanece **MUITO ALTO** só para o dia de migrar a produção de verdade (trocar `site_navbar.html`) -- o que foi feito aqui é a base V2 funcional em ambiente isolado, não a migração real | ✅ Concluído no isolamento V2 (B.6E); migração real pra produção continua fase própria futura |

## Leitura da matriz

- **Nenhuma linha desta tabela está aprovada para execução.** É um mapa de decisão, não um backlog.
- Risco "MUITO ALTO"/"ALTO" concentra-se em: Header/Footer (compartilhados por tudo), páginas de curso e landing (templates únicos compartilhados por 5 clusters × 6 idiomas, mesmo padrão de risco já comprovado na missão PLE desta sessão), formulários que geram lead ou medem conversão, e o seletor de idioma.
- Risco "BAIXO"/"MÉDIO" concentra-se em blocos estáticos ou puramente apresentacionais (FeatureCard, ProcessSteps, Breadcrumb).
- Linhas marcadas "aguarda aprovação/captação" (Testimonials, VideoSection, Professores) não têm dado real disponível hoje -- migrar o componente visual antes disso só trocaria um vazio por outro; a dependência real é de conteúdo, não de código.
- **Fase B.6E**: além do seletor de idioma (linha atualizada acima), criado um preview isolado da página B2B em `/design_system_v2_b2b` (ver `24-b2b-v2-page-plan.md`) -- não substitui `/empresas` nem o formulário de intenção da linha 19 (`public_intent_page*.html`/`public_funnel.submit_public_intent`), que continua real/ativo em produção sem nenhuma alteração.
